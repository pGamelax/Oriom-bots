:80 {
    # Proxy /api/* para o backend (usa env var nativamente)
    reverse_proxy /api/* {$VITE_API_URL}

    # Servir arquivos estáticos com fallback SPA
    root * /srv
    file_server
    try_files {path} /index.html
}
